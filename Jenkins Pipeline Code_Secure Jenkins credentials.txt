pipeline
{
Agent any 
    {
     stages
        {   
       stage('Secret')
        {
          steps {
                WithVault ([
                     VaultSecrets[path :  /api/key/secret , secretValues { envvar : API_KEY , VaultKey :token }
                ])

            echo sh - D Authorization: Bearer $API_KEY" https://api/test
                    
}
}
}
}
}

        } 